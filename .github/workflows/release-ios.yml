name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  validate:
    name: Validate Podspec
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4

      - name: Lint podspec
        run: pod lib lint UseSenseSDK.podspec --allow-warnings

  build-xcframework:
    name: Build XCFramework
    runs-on: macos-15
    needs: validate
    steps:
      - uses: actions/checkout@v4

      - name: Build for iOS device
        run: |
          xcodebuild build \
            -scheme UseSenseSDK \
            -destination 'generic/platform=iOS' \
            -derivedDataPath build/device \
            -skipPackagePluginValidation \
            BUILD_LIBRARY_FOR_DISTRIBUTION=YES \
            CODE_SIGNING_ALLOWED=NO

      - name: Build for iOS Simulator
        run: |
          xcodebuild build \
            -scheme UseSenseSDK \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath build/simulator \
            -skipPackagePluginValidation \
            BUILD_LIBRARY_FOR_DISTRIBUTION=YES \
            CODE_SIGNING_ALLOWED=NO

      - name: Create XCFramework
        run: |
          DEVICE_FW=$(find build/device -name 'UseSenseSDK.framework' -path '*/Release-iphoneos/*' | head -1)
          SIM_FW=$(find build/simulator -name 'UseSenseSDK.framework' -path '*/Release-iphonesimulator/*' | head -1)

          echo "Device framework: $DEVICE_FW"
          echo "Simulator framework: $SIM_FW"

          xcodebuild -create-xcframework \
            -framework "$DEVICE_FW" \
            -framework "$SIM_FW" \
            -output build/UseSenseSDK.xcframework

      - name: Zip XCFramework
        run: |
          cd build
          zip -r UseSenseSDK.xcframework.zip UseSenseSDK.xcframework

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: UseSenseSDK-xcframework
          path: build/UseSenseSDK.xcframework.zip

  publish-cocoapods:
    name: Publish to CocoaPods
    runs-on: macos-15
    needs: validate
    steps:
      - uses: actions/checkout@v4

      # Requires COCOAPODS_TRUNK_TOKEN secret configured in repository settings.
      # Generate a token with: pod trunk register <email> '<name>'
      - name: Push to CocoaPods Trunk
        env:
          COCOAPODS_TRUNK_TOKEN: ${{ secrets.COCOAPODS_TRUNK_TOKEN }}
        run: pod trunk push UseSenseSDK.podspec --allow-warnings

  create-release:
    name: Create GitHub Release
    runs-on: macos-15
    needs: build-xcframework
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download XCFramework
        uses: actions/download-artifact@v4
        with:
          name: UseSenseSDK-xcframework
          path: build/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: build/UseSenseSDK.xcframework.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
