name: Release

# Triggered automatically by pushing a `v*` tag (e.g. `v4.4.0`).
# Also runnable manually via `gh workflow run release-ios.yml --ref <tag>`.
#
# Publish order matters: UseSenseSDK now depends on UseSenseMediaPipe (the
# vendored, Info.plist-patched MediaPipe frameworks), and UseSenseMediaPipe's
# podspec downloads its binaries from THIS tag's GitHub Release. So the pipeline
# is strictly sequenced:
#   vendor-mediapipe + build-xcframework  ->  create-release (attaches both zips)
#     ->  publish-mediapipe (pod trunk push)  ->  publish-sdk (pod trunk push)
# Each `pod trunk push` lints first, so a misconfiguration FAILS the release
# rather than publishing a broken pod.
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  vendor-mediapipe:
    name: Build patched MediaPipe bundle
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v4
      - name: Vendor + patch MediaPipe xcframeworks
        run: bash scripts/vendor-mediapipe.sh "$PWD/build/mediapipe"
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: UseSenseMediaPipe-xcframeworks
          path: build/mediapipe/UseSenseMediaPipe.xcframeworks.zip

  build-xcframework:
    name: Build XCFramework
    runs-on: macos-15
    # The XCFramework attached to the GitHub Release is built from a dedicated
    # Xcode framework target (XCFrameworkBuild/project.yml) generated via
    # xcodegen at CI time. See that file for why the SwiftPM product can't be
    # archived into a consumable .framework directly.
    steps:
      - uses: actions/checkout@v4

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate wrapper Xcode project
        run: |
          cd XCFrameworkBuild
          xcodegen generate

      - name: Archive for iOS device
        run: |
          xcodebuild archive \
            -project XCFrameworkBuild/UseSenseSDK.xcodeproj \
            -scheme UseSenseSDK \
            -configuration Release \
            -destination 'generic/platform=iOS' \
            -archivePath build/ios.xcarchive \
            SKIP_INSTALL=NO \
            BUILD_LIBRARY_FOR_DISTRIBUTION=YES \
            CODE_SIGNING_ALLOWED=NO

      - name: Archive for iOS Simulator
        run: |
          xcodebuild archive \
            -project XCFrameworkBuild/UseSenseSDK.xcodeproj \
            -scheme UseSenseSDK \
            -configuration Release \
            -destination 'generic/platform=iOS Simulator' \
            -archivePath build/sim.xcarchive \
            SKIP_INSTALL=NO \
            BUILD_LIBRARY_FOR_DISTRIBUTION=YES \
            CODE_SIGNING_ALLOWED=NO

      - name: Create XCFramework
        run: |
          DEVICE_FW=build/ios.xcarchive/Products/Library/Frameworks/UseSenseSDK.framework
          SIM_FW=build/sim.xcarchive/Products/Library/Frameworks/UseSenseSDK.framework

          if [ ! -d "$DEVICE_FW" ] || [ ! -d "$SIM_FW" ]; then
            echo "ERROR: Framework not found at expected archive path."
            echo "Device: $DEVICE_FW ($(if [ -d "$DEVICE_FW" ]; then echo ok; else echo missing; fi))"
            echo "Sim:    $SIM_FW ($(if [ -d "$SIM_FW" ]; then echo ok; else echo missing; fi))"
            find build -name 'UseSenseSDK*' -type d 2>/dev/null
            exit 1
          fi

          xcodebuild -create-xcframework \
            -framework "$DEVICE_FW" \
            -framework "$SIM_FW" \
            -output build/UseSenseSDK.xcframework

      - name: Zip XCFramework
        run: |
          cd build
          zip -r UseSenseSDK.xcframework.zip UseSenseSDK.xcframework

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: UseSenseSDK-xcframework
          path: build/UseSenseSDK.xcframework.zip

  create-release:
    name: Create GitHub Release
    runs-on: macos-15
    needs: [vendor-mediapipe, build-xcframework]
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Download SDK XCFramework
        uses: actions/download-artifact@v4
        with:
          name: UseSenseSDK-xcframework
          path: build/
      - name: Download patched MediaPipe bundle
        uses: actions/download-artifact@v4
        with:
          name: UseSenseMediaPipe-xcframeworks
          path: build/
      - name: Create Release
        # UseSenseMediaPipe.podspec downloads its binaries from this asset, so the
        # release MUST exist before the CocoaPods publish jobs run.
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            build/UseSenseSDK.xcframework.zip
            build/UseSenseMediaPipe.xcframeworks.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  publish-mediapipe:
    name: Publish UseSenseMediaPipe to CocoaPods
    runs-on: macos-15
    needs: create-release
    # Must publish before UseSenseSDK, which declares it as a dependency.
    steps:
      - uses: actions/checkout@v4
      - name: Push UseSenseMediaPipe to CocoaPods Trunk
        env:
          COCOAPODS_TRUNK_TOKEN: ${{ secrets.COCOAPODS_TRUNK_TOKEN }}
        # Idempotent + resilient to CocoaPods trunk flakiness. Two real failure
        # modes this guards against (both observed in prod):
        #   1. The skip-check pattern starts with "-", so plain `grep -qF "- $VER ("`
        #      errors with "invalid option" and never skips. Use `grep -qF --`.
        #   2. `pod trunk push` frequently times out calling GitHub's commit API
        #      *after* the spec has already been committed, exiting non-zero on a
        #      successful publish. So on a non-zero push we re-check trunk and
        #      treat "now present" as success, and retry a couple of times.
        run: |
          VER=$(grep -m1 's.version' UseSenseMediaPipe.podspec | sed -E "s/.*'([^']+)'.*/\1/")
          on_trunk() { pod trunk info UseSenseMediaPipe 2>/dev/null | grep -qF -- "- $VER ("; }
          if on_trunk; then echo "UseSenseMediaPipe $VER already on trunk; skipping."; exit 0; fi
          for attempt in 1 2 3; do
            echo "Attempt $attempt: pod trunk push UseSenseMediaPipe $VER"
            if pod trunk push UseSenseMediaPipe.podspec --allow-warnings --synchronous; then exit 0; fi
            echo "push exited non-zero; CocoaPods may have timed out AFTER committing — re-checking trunk..."
            sleep 30
            if on_trunk; then echo "UseSenseMediaPipe $VER landed on trunk despite the error; treating as success."; exit 0; fi
            echo "not on trunk yet; backing off before retry..."
            sleep 30
          done
          echo "::error::UseSenseMediaPipe $VER failed to publish after 3 attempts"
          exit 1

  publish-sdk:
    name: Publish UseSenseSDK to CocoaPods
    runs-on: macos-15
    needs: publish-mediapipe
    steps:
      - uses: actions/checkout@v4
      - name: Push UseSenseSDK to CocoaPods Trunk
        env:
          COCOAPODS_TRUNK_TOKEN: ${{ secrets.COCOAPODS_TRUNK_TOKEN }}
        # --synchronous: UseSenseMediaPipe was just pushed; the CocoaPods CDN
        #   version index can lag minutes/hours behind trunk, so a normal push
        #   fails to resolve the dependency. --synchronous validates against
        #   trunk's authoritative store instead of the lagging CDN index.
        # --skip-import-validation: the validation builds a synthetic app that
        #   links MediaPipe's STATIC binaries under dynamic frameworks, which
        #   fails ("transitive dependencies that include statically linked
        #   binaries"). Real integration uses static linkage and is covered by the
        #   Example app CI build, so skip the import step here.
        # Idempotent + resilient (same guards as the MediaPipe push): `grep -qF --`
        # so the leading-"-" skip-check works, and on a non-zero push re-check
        # trunk so a GitHub-commit-API timeout *after* a successful commit isn't
        # reported as a failure; retry a couple of times for genuine transients.
        run: |
          VER=$(grep -m1 's.version' UseSenseSDK.podspec | sed -E "s/.*'([^']+)'.*/\1/")
          on_trunk() { pod trunk info UseSenseSDK 2>/dev/null | grep -qF -- "- $VER ("; }
          if on_trunk; then echo "UseSenseSDK $VER already on trunk; skipping."; exit 0; fi
          for attempt in 1 2 3; do
            echo "Attempt $attempt: pod trunk push UseSenseSDK $VER"
            if pod trunk push UseSenseSDK.podspec --allow-warnings --synchronous --skip-import-validation; then exit 0; fi
            echo "push exited non-zero; CocoaPods may have timed out AFTER committing — re-checking trunk..."
            sleep 30
            if on_trunk; then echo "UseSenseSDK $VER landed on trunk despite the error; treating as success."; exit 0; fi
            echo "not on trunk yet; backing off before retry..."
            sleep 30
          done
          echo "::error::UseSenseSDK $VER failed to publish after 3 attempts"
          exit 1
